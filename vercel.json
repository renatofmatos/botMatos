{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node",
        "config": {
          "startCommand": "npm start"
        }
      }
    ],
    "routes": [
      {
        "src": "/api/webhook",
        "dest": "/api/webhook",
        "methods": ["POST"]
      }
    ]
  }