{
    "version": 2,
    "builds": [
        {
            "src": "package.json",
            "use": "@vercel/node",
            "buildCommand": "npm run build"
        }
    ],
    "routes": [
        {
            "src": "/webhook",
            "dest": "/api/webhook"
        }
    ]
}